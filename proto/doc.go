// Package proto holds the data and wire types shared between the broker and
// the SDK. It is the only module both sides may import; nothing in proto may
// import broker internals or SDK code.
package proto
